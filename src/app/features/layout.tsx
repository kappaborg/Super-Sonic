import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Voice Authentication Features | SecureSonic',
    description: 'Explore the advanced voice authentication features that SecureSonic offers for secure online meetings.',
};

export default function FeaturesLayout({ children }: { children: React.ReactNode }) {
    return children;
} 